---
description: Create a new debug version for this application
---

First, check if docs/CREATE_NEW_DEBUG_VERSION.md exists.

If it does NOT exist, run the setup flow:
- Ask the user which bat file to run to create a debug version
- Once determined, write the instructions to docs/CREATE_NEW_DEBUG_VERSION.md so next time this step is skipped

Once docs/CREATE_NEW_DEBUG_VERSION.md exists (either already or just created), read it and create a new debug version.

