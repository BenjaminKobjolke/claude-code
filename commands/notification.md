---
description: Configure notification sounds and taskbar flash for Claude Code events
---

# Notification Setup

Launch the interactive notification configuration in a new terminal window.

Run this command:

`/xida:launch ${CLAUDE_PLUGIN_ROOT}/notification/setup.sh`
