---
description: Setup the process to build a debug version for this application
---

Check docs/CREATE_NEW_DEBUG_VERSION.md if it exists and contains infos how creating new debug version works. If not ask the user about it.

We need to know which bat file to run to create a debug version.
If you figured it out together with the user than write it to docs/CREATE_NEW_DEBUG_VERSION.md so next time you can skip this step and directly build it.